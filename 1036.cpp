#include <bits/stdc++.h>
using namespace std;

int main()
{
    int one, two, three;
    cin >> one >> two >> three;
    if ((one + two) > three)
    {
        cout << "yes";
    }
    else
    {
        cout << "no";
    }
    return 0;
}